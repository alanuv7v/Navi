1. depth+ 할 때는 그보다 depth가 한단계 낮은 head가 있어야 한다. 즉 부모가 있어야 한다. 그렇지 않으면 depth+는 취소된다.
block의 순서를 바꿀 때도 마찬가지이다. 순서를 바꾼 결과가 이상하면 안된다.
! 다만 이걸 각 동작을 취하는걸 금지하지 말고, 그냥 문제가 있는 Block을 붉게 하이라이트 하고 이유를 적어주자.
문제 있는 상태에서는 Block들은 따로 저장이 되지만(docEditSession), YAML로 해석되어 저장되진 못한다.

Block이 가질 수 있는 문제들:
1. 부모 없음(depth 한단계 낮은 상위 Block 없음)
2. 


rule을 검사할 방법:
각 블록에 대한 부모 존재여부 검사를 하면 되지.
블록 previousSibling의 depth를 구한다.


